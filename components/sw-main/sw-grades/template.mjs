import { FRONTEND } from "/global.mjs";
const template = document.createElement("template");

template.innerHTML = `
    <link rel="stylesheet" href="${FRONTEND}/components/sw-main/sw-grades/shadow.css">
    TODO: grades and overall grade per student
`;

export default template;